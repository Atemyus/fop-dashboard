[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration for persistent data
# Uncomment these lines after creating the volume on Railway Dashboard
# [[deploy.volumes]]
# mountPath = "/data"
