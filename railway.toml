[build]
builder = "nixpacks"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration for persistent data
# The volume will be mounted at /data
# This directory will persist across deployments
[[deploy.volumes]]
mountPath = "/data"
